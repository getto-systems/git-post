#!/bin/bash

git_post_github(){
	local data
	data='{"title": "'$current'", "head": "'$current'", "base": "'$head_branch'"}'

	curl -i -H "Authorization: token $GIT_POST_GITHUB_ACCESS_TOKEN" "https://api.github.com/repos/$destination/pulls" -d "$data"
}
